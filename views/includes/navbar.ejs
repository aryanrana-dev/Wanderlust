<style>
    .search-btn {
    background-color: #fe424d;
    color: white;
    border-radius: 20px;
    padding: 0px 15px;
    margin-left: 1rem;
}
.search-btn:hover {
    background-color: #fe424d;
    color: white;
}
.search-btn i {
    display: inline;
    margin-right: 5px;
}
.search-input {
    border-radius: 20px;
    padding: 5px 25px;
    border-color: grey;
    text-align: center;
}
</style>
<nav class="navbar navbar-expand-md bg-body-white sticky-top border-bottom">
    <div class="container-fluid">
        <a class="navbar-brand" href="#" style="margin-left: 10px; padding: 0px;"><i
                class="fa-solid fa-compass"></i></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link" href="/listings" style="margin-left: 0px; font-size: larger;"><b>Explore</b></a>
            </div>
            <div class="navbar-nav search-bar ms-auto">
                <form class="d-flex" role="search">
                    <input class="form-control me-2 search-input" type="search" placeholder="Search destinations" aria-label="Search" />
                    <button class="btn btn-outline-secondary search-btn" type="submit"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
                </form>
            </div>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/listings/new">Post your listing</a>
                <% if(!currentUser) { %>
                    <a class="nav-link" href="/signup">Sign Up</a>
                    <a class="nav-link" href="/login">Log In</a>
            </div>
            <% } else { %>
                <div class="navbar-nav ms-auto">
                    <a class="nav-link" href="/logout">Log Out</a>
                    <% } %>
                </div>
        </div>
    </div>
</nav>